node_modules/
dist/
.rtv-store/
*.tsbuildinfo
docs
docs-local/
.rtv-hub/

# Personal/local-only scripts and configs (do NOT publish)
scripts/local/
scripts/seed-config.json

# Claude Code per-machine local settings (permission allowlists etc.)
.claude/settings.local.json

# Local env (hub token etc.) — never commit
.env
.env.local
